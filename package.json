{
  "name": "crev",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "crev": "./dist/bin/cli.js"
  },
  "files": ["dist", "skills", "agents"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "dev": "tsx bin/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
